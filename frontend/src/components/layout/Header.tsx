import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ padding: '1rem', borderBottom: '1px solid #ccc', backgroundColor: '#f8f8f8' }}>
      <h1>BalancedNews</h1>
      {/* Navigation links can be added here later */}
    </header>
  );
};

export default Header;